# Dev-team memory log

## Standing notes

**Repo layout / commands**
- Turbo root `pnpm lint` / `pnpm typecheck` are BROKEN ("cannot find binary path") — run per-package from `apps/web`.
- **Run the web suite with `corepack pnpm test` from the repo root** (Turbo: builds first, then tests — `turbo.json` `test` task is `dependsOn: ["build"]`, `cache: false`). `corepack pnpm --filter @nseluga/web test` runs it without building. Both exit non-zero on failure (verified by probe injection). Expect **12 files / 52 tests / 0 fail / 0 skip** with a build present; without a build, 51 pass + 1 skip and b3 reports 3 skipped sections — that is the correct clean-tree result, not a regression.
- Underlying command is still `cd apps/web && node --experimental-strip-types --test __tests__/*.mjs`; the `--experimental-strip-types` flag is REQUIRED (tests import `lib/content.ts` directly). Node 22.14. Deps via `corepack pnpm install`. Do NOT add `"type": "module"` to `apps/web/package.json` despite Node's warning — it breaks the CJS `postcss.config.js`/`tailwind.config.js` the Next build needs.
- `templates/*` IS in the pnpm-workspace glob (installable member) alongside `apps/*` and `packages/*`.
- Packages are `@nseluga/*` (ui, config, app-core) — older notes said `@bcns/*`; the scope was renamed.
- DeLuca's app was extracted out of this repo to `~/bcns-client-delucas`. L2 Detailz lives in `~/bcns-client-l2detailz`.

**Test suite (all 12 files GREEN as of 2026-07-27 — the stale-test backlog below is CLEARED)**
- The registry has NO `problemSolution`, NO `deliveryModels`, NO `aboutFounder` — removed/restructured in B2/B3, intentional. Successors: `siteContent.about` + `about.founders[]` (array; each has `name, roleLine, photo, bio, credentials`). Never re-add the old keys to make a test pass.
- `pastWork.items` and `reviews.items` are intentionally EMPTY; each section renders a `holdingState` instead. Tests assert "non-empty items OR populated holdingState", so they keep passing when a later item seeds `items`.
- `b3-copy-wiring` and `w3-hosting-explanation` assert against `.next/server/app/*.html`. They skip LOUDLY (stderr `⚠️` + `# SKIP`) when the build is absent — that is why root `pnpm test` depends on `build`.
- Built-HTML literal checks in `b3` run through `stripTags()` first: the layout-loop wraps accent words in inline `<span>`s, so a raw `includes()` on markup breaks even when the copy is correct. Use the same treatment for any new built-HTML copy assertion.
- Registry-wide invariant is now "no string field ships empty" (was "every string is a `[SLOT: ...]` or step number" — obsolete once real copy landed in B3).

**Content conventions**
- `apps/web/lib/content.ts` is the single source of truth for copy; `apps/web/CONTENT.md` is its 1:1 mirror — `w4-content-mirror.test.mjs` gates it.
- APPEND new `faq.items` (never insert) so index-based assertions in `b3` stay green.
- Scope em-dash / copy greps to `content.ts` source or `git diff` — rendered HTML carries pre-existing em-dashes from `site.ts` OG metadata.
- En-dash (`–`) in shell grep silently mismatches; use Python for exact-string checks with Unicode range chars.
- `apps/web/scripts/readability-check.py` exists for copy passes (FK grade).
- `problem-solution.tsx` and `delivery-models.tsx` return `null` — kept but inert.

**Process**
- dt-* agents write `*-report.md` to the working tree but usually do NOT commit it. The orchestrator must `git add .claude/dev-team/` with the outcome commit, and must never `git checkout` a report file.
- Any agent that starts a dev server must background it with a bounded poll and always kill it.
- Every IPC/handler that accepts a key/value pair needs an explicit ALLOWED_KEYS allowlist — recurring gap across every prior build item here.
- Built-HTML Python string checks against `.next/server/app/*.html` are the right un-mocked behavioral gate for content changes.
- Parallel-group branches conflict only on `.claude/dev-team/*-report.md` (add/add) — resolve `--theirs`, not a real overlap.

## Recent runs

## 2026-07-19 — dev-team-auto — A1 @nseluga/app-core package
- **Outcome:** DONE — 1 build + Opus review + 1 Opus fix pass (full track, flag:money, branch dev-team/model-migration-run, commit 99b0b94)
- **What happened:** flag:money → 2 parallel Opus design sketches, both converged on cents-as-integer. Built packages/app-core (pricing/subscription/anthropic), 54 tests green. Review: 1 Important (PRICING not deep-frozen) + 2 Minor.
- **What worked:** New package = copy `packages/ui` shape exactly. Tests via `tsx` with an explicit `&&` chain in the `test` script (bare glob mis-expands). DI seam for the Anthropic client makes it fully mockable.
- **What failed:** PRICING was compile-time-Readonly only — runtime mutable shared constant.
- **Remember next run:** app-core exports PRICING, INCLUDED_SEATS, PER_SEAT_CENTS, formatUsd, monthlyCharge, setupFeeCents, decideAccess, decideFromEvent, createAnthropicClient, DEFAULT_MODEL. Site pricing still hardcodes strings in content.ts.

## 2026-07-19 — dev-team-auto — A2 templates/hosted-web scaffold
- **Outcome:** DONE — 1 build + Opus security review + 1 Opus fix pass (full track, flag:security, branch dev-team/model-migration-run, commit 3a11c62)
- **What happened:** Scaffolded `@nseluga/hosted-web-template` (Next.js App Router, TS strict), added `templates/*` to the workspace glob. QA ran a live smoke on a real dev server at :3100. Security review caught the Stripe webhook FAILING OPEN even with the secret set; fixer made it fail-closed.
- **What worked:** Live smoke on the real server is the right gate for a route item. Template test runner must be `tsx --test` — raw node ESM can't follow app-core's extensionless re-exports.
- **What failed:** Engineer built the webhook stub fail-OPEN in a clonable template. Also a QA agent died mid-run (FailedToOpenSocket) leaving stale reports.
- **Remember next run:** Real Stripe sig verification is still Needs-Nate before prod.

## 2026-07-19 — dev-team-auto — A3 hosted-web ADR
- **Outcome:** DONE — 1 build attempt, QA PASS (light track, no review, commit 3cac7b5)
- **What happened:** Authored `docs/architecture/hosted-web-model.md` from the PLAN decisions table.
- **What worked:** For a docs ADR, a committed node string-presence check (`docs/architecture/__tests__/hosted-web-model.check.mjs`) is a cleaner, more durable gate than a prose read.
- **What failed:** nothing.
- **Remember next run:** The ADR has a `__tests__/` content-check beside it.

## 2026-07-19 — dev-team-auto — A4 repo docs to per-client-repo model
- **Outcome:** DONE — 1 build + QA FAIL + 1 fix pass (light track, no review, commit 49d72eb)
- **What happened:** Reframed CLAUDE.md + README.md to the per-client-repo model. QA FAILED on accuracy — both docs described app-core as "auth, DB, AI, billing", which it is not.
- **What worked:** Adding an explicit ACCURACY criterion to QA ("does the doc's description match what was actually built?") caught a plausible-but-false claim a presence-only grep would have passed.
- **What failed:** Engineer invented capabilities (auth, DB) that don't exist — the "assert what should be true, not what is" family.
- **Remember next run:** Do this on any doc item describing code built earlier in the same run.

## 2026-07-20 — layout-loop — v3 full visual run
- **Outcome:** DONE — merged to main 2026-07-20.
- **What happened:** Fraunces serif accent wired globally, `components/reveal.tsx` (single IntersectionObserver + reduced-motion no-op), `drift`/`shimmer` keyframes in the Tailwind preset, shared hover-lift/glow utilities, `SectionHeading` opt-in serif accent; per-page loops for `/`, `/services`, `/pricing`, `/about`, `/work`.
- **What worked:** A shared Phase-0 foundation before the per-page loops kept one motion vocabulary across all five pages.
- **What failed:** nothing notable.
- **Remember next run:** Any new page MUST reuse `Reveal`, the shared hover-lift/glow utilities, and `SectionHeading` — do not invent a second motion vocabulary or rebuild `signature-motif.tsx`. Brand: one accent hue, pastel-blue `#7CB3FF` on charcoal-purple `#15131F`, no photography. See `LAYOUT_LOOP_REPORT.md`.

## 2026-07-27 21:30 — dev-team-auto — Wire a runnable `test` script for the marketing site
- **Outcome:** DONE — 1 build + 1 QA pass, no fix cycle (light track, branch worktree-past-work-case-studies, commit d47d88c)
- **What happened:** Added `test` to `apps/web/package.json` + root `package.json` and a `test` task to `turbo.json` (`dependsOn: ["build"]`, `cache: false`). Real work was triaging 6 stale test files: re-pointed `aboutFounder` → `about`/`about.founders[]`, dropped `problemSolution`/`deliveryModels`, converted 5 "is a SLOT placeholder" asserts to non-empty-string, replaced `items >= 1` with "items OR holdingState", stripped HTML tags before built-HTML literal checks, and made both build-dependent files skip loudly. 12/12 green.
- **What worked:** Orchestrator adjudicating the baseline FIRST (running every file individually, before and after a build) — the handed-down premise "5 files are stale on problemSolution/deliveryModels" was wrong for 4 of the 5. Only `content-registry` matched it; `a2-*` died on `aboutFounder`, `b2` on filled-in SLOT slots, `b3` purely on the missing build plus one span-wrapped headline. Building first collapsed 10 of 16 failures. **Probe injection** proved all 12 files execute: `node --test`'s TAP names only script-style files, so w1–w4 (real `node:test`) are invisible in output — appending a throwing `test()` to each and confirming `tests 52 → 56, fail 4` is the only honest proof the glob ran them.
- **What failed:** QA ran `git checkout -- apps/web/__tests__/b3-copy-wiring.test.mjs` to revert its own deliberate mutation and destroyed the engineer's uncommitted fix to that file — the exact trap already written in these standing notes. It reconstructed the file by hand; I diffed the reconstruction against the engineer's original (still in my context) and it was faithful, but that was luck, not process. Engineer also left `SLOT_RE.test(v) || STEP_RE.test(v) || v.trim().length > 0` — the first two clauses dead — plus a stale "not a SLOT or step number" failure message; I collapsed both.
- **Remember next run:** Gate the suite with `corepack pnpm test` from the root (see Standing notes for expected counts). When weakening an assertion, ALWAYS mutation-test it — blank the guarded field, confirm the test still fails, restore; the "items OR holdingState" rewrite was the one at real risk of being satisfiable by both being empty (it isn't). Tell every agent to use `cp` backups for temporary mutations, never `git checkout --`, and to verify restores with `shasum -c`.

## 2026-07-27 23:55 — dev-team-auto — Extend PastWorkItem with case-study detail fields (PLAN item 2)
- **Outcome:** DONE — 1 build + QA PASS + review NEEDS_FIXES + 1 fix pass + scoped QA/review (full track, branch worktree-past-work-case-studies, commit f647d4c). **This item was RESUMED mid-loop after the prior orchestrator was interrupted** — its engineer/QA/fix work sat uncommitted in the tree; the resuming orchestrator picked up at the quality gate, applied the review findings, re-gated, and committed. Do not restart a resumed item; read `.claude/dev-team/*-report.md` to find the loop position.
- **What happened:** Added `slug`/`problem`/`approach`/`outcome`/`screenshots[]` to `PastWorkItem`, seeded `pastWork.items` with `delucas` + `l2detailz` (all narrative fields `[INPUT: ...]`), mirrored in CONTENT.md, new `past-work-case-studies.test.mjs`. Review returned 0 Critical / 2 Important / 2 Minor; all four applied; scoped QA + scoped review both green.
- **What worked:** Deriving `b3`'s `APPENDIX_INPUT_TOKENS` from `siteContent` at runtime instead of hand-listing — the hand-synced list had already false-failed once this same pass. Requiring the fixer AND the re-gating QA to each independently probe the derived allowlist with a brand-new apostrophe-bearing token (rebuild, confirm section [3] still passes with zero test edits) proved the self-maintaining property rather than assuming it. Making QA re-count CONTENT.md's cross-check table with its own script instead of trusting the fixer's number caught nothing this time but is the right default for any "the docs say N" fix.
- **What failed:** The engineer left a hand-synced allowlist (third sync point for `[INPUT:` tokens) which cost one fix cycle on an apostrophe/`&#x27;` mismatch — the previous fixer "solved" it by removing apostrophes from the copy rather than decoding entities in the test. CONTENT.md's "Total registry fields: N" prose had been drifting from the actual table for at least two passes (HEAD said 80 with 82 real rows).
- **Remember next run:** `content.ts` embeds `[INPUT: ...]` tokens MID-string in `about.founders[1].bio` and `.credentials[0]` — any token scan must be substring (`/\[INPUT:[^\]]+\]/g`), never whole-string `/^\[INPUT: .+\]$/`, or /about false-fails. Any built-HTML check comparing rendered text to registry strings must entity-decode first (`&#x27;`/`&#39;`/`&quot;`/`&lt;`/`&gt;`/`&amp;`, `&amp;` last) — React escapes rendered text. `b2`/`b3` are script-style files with their own counters, so assertions added there do NOT move node:test's TAP total (still 62) — read their own "Results: N passed" lines instead. Never hand-list a token allowlist in this repo; derive it from `siteContent`. CONTENT.md's "Total registry fields: N" must be re-derived by script (currently 89 rows), never incremented by hand.

## 2026-07-28 00:05 — dev-team-auto — `/work/[slug]` dynamic case-study route (PLAN item 3)
- **Outcome:** DONE — 1 build + QA FAIL + 1 fix + QA PASS + review round-trip + dt-ui + final gate (full track, branch worktree-past-work-case-studies)
- **What happened:** New Server Component route with a single `getCaseStudy(slug)` shared by `generateStaticParams`/`generateMetadata`/the page body. Added `pastWork.caseStudy` labels (later `+backLabel`) to the registry + CONTENT.md. `dynamicParams = false` added after QA found unregistered slugs rendering on demand. dt-ui added a back link and `divide-y` rhythm. Final: 77 pass/0 fail/0 skip, `● SSG` with exactly the 2 registry paths, live 200/200/404, median render 3.4ms.
- **What worked:** Orchestrator personally re-measuring QA's scariest finding instead of accepting either the FAIL or the fixer's explanation — moving the colliding prerendered file aside and re-requesting the exact URL turned "maybe a real 200-instead-of-404 bug" into a proven filesystem artifact in ~3 tool calls. Deriving QA's assertions from `Object.entries(caseStudy)` at runtime meant dt-ui's brand-new `backLabel` field was auto-covered (76→77 tests) with ZERO test edits — the runtime-derivation rule from item 2 paying off directly. Applying the two one-token findings (h3→h2, key) myself instead of spawning dt-fix.
- **What failed:** Review PRESCRIBED `h3` for the section labels in pass 1, then flagged its own prescription as a level-skip in pass 2 — one wasted round-trip. Engineer claimed `w4-content-mirror.test.mjs` gated the new labels; it does not (it only covers pricing/FAQ), so the "mirror is tested" assurance was false until QA wrote the real gate. QA committed its test file (7bf9b68) despite an explicit "do NOT commit".
- **Remember next run:** **macOS/APFS is case-insensitive, so `.next/server/app/<route>/<Slug>.html` resolves to the lowercase prerendered file — any dynamic route on this machine will serve a 200 for a case-variant slug and then corrupt the real prerendered file on write-back.** It 404s correctly on Linux/Vercel; verify with `/work/NoPe` (no colliding file → 404) before believing it is a code bug, and NEVER "fix" it by making slug lookup case-insensitive (that creates a live duplicate URL). `pnpm start` hardcodes `-p 3000`; call `next start -p <free>` directly and kill by PID (`lsof -ti tcp:$PORT`) — `pkill -f "next start"` does not match the `next-server` process name. `SectionHeading` now takes `as?: "h1"|"h2"|"h3"` (default `"h2"`); use `as="h1"` on any new single-topic page — `/work`, `/pricing`, `/services`, `/about` still have NO h1 and are a one-word fix each if that ever matters. CONTENT.md cross-check is now 93 rows.

## 2026-07-28 09:20 — dev-team-auto — Link Past Work cards to `/work/<slug>` (PLAN item 4)
- **Outcome:** DONE — 1 build + 1 QA PASS, no fix cycle (light track, branch worktree-past-work-case-studies, commit 602b8ac)
- **What happened:** Each grid cell became `<div class="flex h-full flex-col">` holding a `<Link href="/work/${slug}" class="block flex-1 rounded-xl ring-offset-background focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2">` around the `<Card>` (now `group hover-lift`), with the optional external `link` anchor moved OUT as a SIBLING. New `past-work-card-links.test.mjs`. 82 pass/0 fail/0 skip.
- **What worked:** Orchestrator prescribing the exact JSX shape in the engineer's spawn prompt — design space was genuinely narrow, and handing it down turned a trap-laden item into a single clean build with no fix cycle. QA's 22-point `document.elementFromPoint()` sweep (11 pts/card: 4 inset corners, 4 edge midpoints, center, the header/content padding gap, over the outcome text) is the check that actually proves "click anywhere navigates" — it runs at CDP level and is immune to the OS-focus flakiness that made real mouse clicks intermittent. Engineer pre-auditing `section-atmosphere.tsx` for `pointer-events-none` on its decorative layers (both already correct) closed the click-stealing-overlay risk before QA ever ran. Holding-state A/B done as a true rebuild-both-legs comparison → `DIFFERING_BYTES: 0`.
- **What failed:** Nothing gate-relevant. Engineer committed despite "do NOT commit" (third agent in a row to do this here — stop bothering to forbid it, just expect a feature commit and add reports/PLAN/PROGRESS on top). `resize_window` silently no-op'd so the 390px mobile screenshot was Not Verifiable; accepted on structure (no width/overflow classes added, grid breakpoints untouched, section root already `overflow-hidden` so `hover-lift`'s `scale-[1.02]` cannot cause horizontal scroll).
- **Remember next run:** **`link` is still live data, not dead code** — PLAN item 9 sets L2 Detailz's URL after DNS cutover, so it renders as a sibling BELOW the card; do not "clean it up". Card click affordance is `hover-lift` on the Card root (the repo's shared utility, same as `use-cases`/`pricing`/`about-founder`) — no new motion vocabulary was invented and none should be. Focus ring `ring-ring` on `bg-card` measures **7.59:1 dark / 5.69:1 light**, so the repo's standard `focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2` + `ring-offset-background` clears AA on any card surface here — reuse it rather than re-deriving. `ring-offset-background` is REQUIRED; without it Tailwind's ring offset defaults to white and looks broken on the dark surface. In this sandbox real mouse clicks are intermittently undelivered when the Chrome window is not OS-frontmost (`document.visibilityState === "hidden"`, ZERO capture-phase events); that signature is distinct from a click-stealing overlay (which delivers the event to the WRONG node) — pair every real-click check with an `elementFromPoint` sweep so an environment artifact can never be mistaken for a defect, or vice versa.

## 2026-07-28 — dev-team-auto — Item 6 (cross-repo): L2 Detailz demo seed fixture
- **Outcome:** DONE — full track, flag:data-path, 1 build + 2 fix cycles. **Different repo:** `~/bcns-client-l2detailz`, branch `worktree-demo-seed`, commit 1319991. Nothing landed in this platform repo.
- **What happened:** Opt-in `supabase/demo-seed.sql` — 16 invented identities, 16 bookings, 12 jobs on 9 distinct HST days of the CURRENT HST month (computed at apply time, not hardcoded, so item 7's screenshots need no calendar navigation and the fixture never goes stale). Applied only via a `DEMO_SEED=1` section in `supabase/local-test/run.sh`; an in-file psql guard makes a bare `psql -f` refuse and write 0 rows.
- **What worked:** The orchestrator pre-scouted the containment surface itself (config.toml absent, `db push` = migrations only, `db reset` = `./seed.sql` only, `run.sh` globs `migrations/*.sql`) in 4 tool calls, skipped dt-analyze/dt-research, and handed the engineer a prescribed design — 1-attempt convergence. It also ran the BASE-COMMIT suite itself before QA, converting 7 inherited failures from a false-FAIL risk into a stated delta gate.
- **What failed:** Two orchestrator prescriptions were wrong and the fixer caught both (a vacuous null-`package_id` check — the inner join drops the row so null never appears; and hard-scoping the render test to `l2detailz_test`, which would have made `pnpm test` destroy the DB `run.sh` owns). Review 1's `\if :{?demo_seed_ok}` tested *definedness*, so `-v demo_seed_ok=0` still applied the whole fixture — caught only by review 2.
- **Remember next run:** **Two findings in that repo need their own items, both out of scope here.** (1) `app/_components/BannerFrame.tsx:69` ships a PUBLIC iframe with `sandbox="allow-same-origin allow-top-navigation-by-user-activation"` instead of `sandbox=""` — security-relevant; that combination lets framed content escape the sandbox and drive top-level navigation. (2) 7 test failures pre-exist at `ce076bf` (banner/hero/route-label copy drift from the "updated banner and google maps" commit) — NOT regressions. Suite baseline there: `489/475/9/5` WITH a `.next` build, `475/463/7/5` without. `tests/banners-behavioral.test.mjs`'s live sandbox pair silently no-op-PASSES (does not skip) with no build, which is what masked the iframe defect in every prior baseline. Fixture usage: `DEMO_SEED=1 bash supabase/local-test/run.sh`. `\quit` exits 0, so every caller must assert rows landed rather than trust the exit code. Demo rows are prefixed `d0c…`/`d0b…`/`d0a…`. Late in a month every demo job renders `completed` — deliberate.

## 2026-07-28 — dev-team-auto — Item 5 (cross-repo): DeLuca's mock-bridge demo seed
- **Outcome:** DONE — full track, flag:data-path, 1 build + 2 quality passes. **Different repo:** `~/bcns-client-delucas`, branch `feat/demo-seed-fixture`, final verified commit 60c1247 (the agent's own memory entry cites an earlier checkpoint, 5915bdd). Nothing landed in this platform repo.
- **What happened:** `VITE_DEMO_SEED=1 pnpm dev` overlays the browser mock bridge with a deterministic LCG-generated dataset (12 months × 23 tx, all six categories) derived through the real `src/shared/pnl.ts`. Two Opus review rounds produced 0 Critical / 10 Important / 10 Minor, all applied.
- **What worked:** Deriving all four Dashboard reads from ONE invented transaction array through the existing pure P&L module — no hand-written `MonthPnl`, so the fixture cannot drift from the math. Making "default behaviour unchanged" true BY CONSTRUCTION (spread the base bridge, override only four reads, never edit `mockBridge.ts`) rather than by argument. Standing up a pristine base-commit worktree on :3002 for the A/B before-leg instead of `git stash` over uncommitted work — that is the right pattern for any "must be unchanged" criterion.
- **What failed:** `computeMonthPnl(month, txs)` does NOT filter by month — it sums whatever array it is handed. The first `getMonthPnl` passed the whole dataset and July showed $281,418.69. No test caught it; only driving the UI did. Two dt-* agents returned findings in their final message WITHOUT writing their report file, so the fixer flew blind until the orchestrator transcribed the report from its own context. A reviewer's prescribed constant (`span >= 100_000`) was arithmetically wrong and would have redded the suite.
- **Remember next run:** Anything reading `computeMonthPnl` must hand it a pre-sliced month bucket. Vite `define` in `electron.vite.config.ts` beats both shell env and `.env` for `import.meta.env.*`, so it is the right place to hard-pin a dev-only flag off for production (an `env -u` script prefix would not survive Windows packaging). **Add "write your report to `.claude/dev-team/<name>.md` as well as returning it" to every dt-* spawn prompt** — this is now the second run where a report-file omission cost a cycle. Fixture bounds are hand-typed, guarded only by a `% 1000 === 0` roundness rule + 25,000-cent minimum span, so a ROUND real figure (a $5,500 rent) would still pass — keep bound edits under review. Env fix needed on a fresh clone: `better-sqlite3` ships compiled for Node 20 and the machine runs Node 22; every DB-backed suite fails until it is rebuilt.
