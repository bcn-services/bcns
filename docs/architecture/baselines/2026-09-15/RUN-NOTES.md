# platform-v1 run notes — 2026-09-15/16 (Fable 5.1, unattended)

One line per decision or correction. Repo/git history is not restated.

- bcn-services/bcns is PUBLIC; Nate (03:50 UTC 09-16): "need them all to be public so they can use free github minutes". Old repos flipped public only after a clean history secret scan; else morning step.
- platform-v1 branched from origin/main 6188584 (packages already @bcn-services/*). site/bcns-connect left untouched; its uncommitted CLAUDE.md edit is in `git stash` (stash@{0}) and re-applied on this branch.
- ~~Droplet ufw admits 80/443 only from Cloudflare ranges — needs `ufw allow 80,443/tcp`~~ **Corrected by the chunk 0 verifier (Opus 5):** `droplet/before.txt` ufw rule [17] already allows `80,443/tcp` from Anywhere (`# temp: pre-Cloudflare launch`). No ufw change is made in this run; `bootstrap.sh` keeps the Cloudflare-only loop and gains a comment.
- `/etc/letsencrypt/live` is not empty at baseline (README + `l2details.com` dir; certbot 2.9.0) — l2details.com already has a Let's Encrypt cert on disk even though its vhost serves the Cloudflare origin cert. Never touched.
- Certbot bootstrap order in onboard-client.sh: http vhost → reload → certonly --webroot → https vhost → reload (a vhost citing a missing cert fails nginx -t).
- No Resend key exists; hub degrades to mailto. GH_PACKAGES_TOKEN missing from bcns secrets. Both = morning steps.
- Per-chunk PRs target platform-v1 and are merged onto it locally (`git merge --no-ff` + push) once their verifier passes; only platform-v1 → main is Nate's.
- Chunk 1 gate (c): `supabase db diff --linked` hangs headless (keyring db password, no tty) — proven instead by Supabase MCP `list_migrations` == branch files == baseline (`pv1-gates/gate-c-migrations.txt`); the tty run is a morning step.
- Vercel preview of pv1/1-merge failed: the wizard's Build Command uses the pre-rename name `@nseluga/web` (`pv1-gates/vercel-preview.txt`). Dashboard fix is a morning step; README/CLAUDE.md now state the exact command. Gate (d) stands on the local capture (`pv1-gates/gate-d.txt`).
- Nate's uncommitted bcns-data DESIGN.md/REQUIREMENTS.md edits carried into `platform/` (78cdda9); `~/bcns-data` working tree left as-is (morning: discard them there).
- Dependency drift accepted (dev-only, dedup to root): eslint 9.39.5→9.39.4, @types/node 22.20.2→22.20.1, tsx 4.23.13→4.23.1; SB data-client registry 0.2.0 → workspace 0.3.0 (tests/typecheck/build green).
- Chunk 1 checkpoint: dt-qa (Sonnet 5) PASS; Opus 5 reviewer PASS with 8 nits (stale gate-a.txt, app-core wording, aws-sdk drift line, uncited d.ts diff, worker dry tick unproven → morning step, normalizer missing-route hole → hardened, deploy-app matrix names connect, QA under-reported app-core) — all addressed before PR B opened.
- Chunk 2: Opus 5 infra review of 7dec700 = SAFE TO RUN + 6 should-fix; all applied in 864c132/57bba92 (idempotent useradd, env/vhost guards, renewal deploy hook, comment-safe registry). Live 00-default already had the :80 default_server 444 block; repo heredoc realigned (md5 3ec5a9fe…). l2details.com renewal is webroot, not standalone.
- Chunk 2 droplet: sb/connect/mcp onboarded with Let's Encrypt certs (expire 2026-12-15); deploy key logs in as all three slugs; connect.env appended to /srv/connect/env (never printed). l2detailz vhost/unit/00-default md5 identical before/after. Gate (g) TLS half proven pre-deploy (502 until chunk 4 deploys apps).
- Chunk 3: Opus 5 review of 1ee5524 = FAIL (3 MUST-FIX: redirects dropped ssr Set-Cookie, template pin fail-open when EXPECTED_CLIENT_ID unset, R39 vacuous at 0 rows; 9 SHOULD-FIX) → all fixed in e17fa4b; re-review PASS. Re-QA found /api/health still 200 with the pin missing (outside the matcher) → ea54fdc reports `tenant_pin_missing`/503 so the deploy rolls back; proven in `pv1-gates/gate-h.txt`.
- Chunk 3: `EXPECTED_CLIENT_ID` is operator-set from `clients.id` (new-app.sh cannot know it); SB keeps the pin optional until `/srv/sb/env` has it (morning step). os skill rename is bcns-os PR #2.
- chunk 4 (2026-09-16): first SB droplet deploy exposed tenant middleware redirecting to https://localhost:3101/login (request.url is the internal origin behind nginx). Fixed in packages/tenant (Host header, X-Forwarded-Host ignored) + 2 tests, PR pv1/4a-redirect-host; SB redeployed and 307 → https://sb.bcn-services.com/login verified. /srv/sb/env now carries DATA_SOURCE=shared, hosted URL/anon, EXPECTED_CLIENT_ID=ae196fbf…, HEALTH_EMAIL/PASSWORD (keychain bcns-smoke-sb).
- chunk 4: `service_role` has no USAGE on `api`/`data` and PostgREST exposes `api` only, so the Edge Functions cannot write memberships with the service key. Added `api.add_member(uuid, text)` security-definer RPC (tenant + role from the caller's own membership row, not the JWT claim; 'member' only; ON CONFLICT guarded on client_id). Not in the plan; alternative (grant service_role USAGE on data) rejected as a larger blast radius.
- chunk 4: Opus 5 review PASS with 5 MEDIUM/5 LOW/3 INFO; QA (Sonnet 5) FAIL #1 = platform `allowImportingTsExtensions` leaked into data-client emit (TS5096). All fixed in b3267be (data-client tsconfig override, RPC hardening, pinned supabase-js 2.116 + admin-users email filter, AbortSignal timeouts, log hygiene). Guards proven in a rolled-back txn incl. a forged owner claim → BCNS2.
- chunk 4: `/team` hides `is_smoke` rows from members and `/access` is owner-only, so gate (i) captures both the member view and an owner view (local fixture promoted then reverted). `tmp/gate-i.sh` is the reproducible runner. Hub deployed to the droplet at b3267be; gate (g) recaptured; l2detailz md5s unchanged.
- chunk 4: no Resend key, META_*/MONDAY_* absent → every source button is "Request connection" (mailto fallback logs slug/source only). deno not installed locally → Edge Function index.ts wiring checked statically; handlers unit-tested (19 vitest cases).
- chunk 4 CI fix: platform-ci was red on 48b022f (no RPC_ARGS entry for add_member — CI's fresh supabase start applies the migration, local stack had it rolled back). Added RPC_ARGS.add_member (BCNS3 other_client); local 161|1 todo with the migration applied; evidence pv1-gates/chunk4-ci-fix-add-member.txt.
