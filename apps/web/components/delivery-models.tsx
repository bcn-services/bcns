// Section cut from site IA — component kept in repo but not used on any page.
// deliveryModels were merged into /pricing tiers and /services copy.

export function DeliveryModels() {
  return null;
}
